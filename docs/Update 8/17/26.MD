# Project Update - August 17, 2026

## Security Hub Account Management

- Added a dedicated revoked-account state using the nullable `users.revoked_at` timestamp.
- Separated staff accounts into three distinct lists:
  - Pending accounts are registered staff awaiting their first approval.
  - Active accounts are approved staff with system access.
  - Revoked accounts are previously active staff whose access was removed.
- Consolidated the three account lists into a single **Account Management** card with Pending, Active, and Revoked tabs.
- Added count badges to each account tab.
- Replaced inline account buttons with ellipsis action dropdowns:
  - Pending: Approve or Decline.
  - Active: Revoke Access.
  - Revoked: Restore Access or Delete Account.
- Action dropdowns close when another tab is selected or the user clicks elsewhere.

## Revoked Account Workflow

- Revoking an active staff account now sets `is_approved` to `false` and records `revoked_at`.
- Revoked accounts no longer return to Pending Approvals.
- Restoring access sets `is_approved` to `true` and clears `revoked_at`.
- Revoked accounts can be permanently deleted from their action dropdown.
- Restore and delete endpoints reject pending, active, and administrator accounts.
- Audit-log entries and success messages were added for revoke, restore, and permanent deletion actions.
- Existing unapproved accounts remain classified as pending after migration.

## Authentication Feedback

- Pending users continue to receive the awaiting-administrator-approval message.
- Revoked users now receive a separate message stating that an administrator revoked their access.
- Active sessions are still terminated when access is revoked.

## Confirmation and Form Behavior

- Replaced the browser-native Revoke Access confirmation dialog with a styled in-page confirmation card.
- The card displays the selected staff member and the consequences of revocation.
- It can be dismissed through Cancel, the close button, backdrop click, or the Escape key.
- Cancelled form confirmations no longer leave submit buttons in the animated Processing state.

## Sales Interface

- Removed the clear "x" from the selected **Select Item from Inventory** dropdown while retaining searchable Select2 behavior.
- Made the Peak Hour Tracer Weekly view functional using Monday-through-Sunday totals from the current calendar week.
- Added complete dark-mode styling for the Select2 inventory picker, including its search field, options, highlighted option, and selected option.

## Detailed System Activity Auditing

- Added a centralized `SystemAudit` recorder that retains the actor name, role, IP address, module, action type, subject, and structured action details.
- Replaced generic inventory activity messages with specific records for:
  - Product creation, editing, and deletion.
  - Restocks with supplier, delivery reference, quantity, and before/after stock.
  - Excel imports with filename and created/updated/processed counts.
- Added detailed sales records containing receipt number, product/SKU, customer, quantity, total amount, and remaining stock.
- Added audit records for DSS markdowns and report generation, opening, downloading, and deletion.
- Added actor-attributed authentication, password reset, profile, and settings records.
- Updated the Security Hub trail to show Admin, Staff, or System badges and module labels.
- Added server-side filtering by keyword, actor role, module, and time period.
- Filters now apply automatically: dropdowns submit immediately and keyword search uses a short debounce.
- Keyword search also matches module names.
- Made the Account Management card sticky on desktop while the audit trail scrolls.

## Profile and Navigation

- Added an authenticated Profile page for editing name, email, profile photo, and password.
- Profile photos are stored privately and served through an authenticated route; the stored database path uses the encrypted user cast.
- Added the upper-right profile dropdown for Profile, Settings, and Sign Out.
- Removed the duplicate sidebar logout action.
- Added the admin-only Security Hub destination to global search.
- Removed the global-search loading-button animation.
- Removed Last Activity and Account Role from the Profile page.

## Application Settings

- Added a dedicated authenticated Settings page with encrypted per-account preferences.
- Added Light, Dark, and Follow System appearance modes.
- Added role-aware preferred landing pages and persisted expanded/collapsed sidebar state.
- Added strict Reduced Motion mode that disables animations, transitions, pulsing, spinning, and smooth scrolling while keeping all content visible.
- Removed the Navigation and Search Density preference and its compact-mode behavior.
- Normalized dark-mode surfaces, text, borders, form controls, badges, selected states, and hover/focus colors across authenticated pages.

## Reports and Inventory

- Removed Custom Date Range from report timeframes.
- Corrected quarter reporting to use three calendar months per quarter and stored exact inclusive period boundaries with each generated report.
- Quarter choices now stop at the current quarter, so future quarters are not shown or accepted.
- Report types now query data using the stored report period rather than inferring dates from report titles.
- Removed the Inventory Print Stock Report button and aligned Import Excel and Add New Product actions to the right.

## Database

- Added and applied migration `2026_08_16_200000_add_revoked_at_to_users_table`.
- Added migration `2026_08_17_000000_add_profile_photo_path_to_users_table`.
- Added migration `2026_08_17_010000_add_preferences_to_users_table`.
- Added migration `2026_08_17_020000_add_timeframe_fields_to_reports_table`.
- User profile photo paths and application preferences use encrypted casts.

## Verification

- Added feature coverage for revoked accounts, profiles, settings, report quarters, weekly sales tracing, and detailed system auditing/filtering.
- Focused feature suites pass for the implemented workflows.
- Frontend assets compile successfully with Vite.
- Updated the starter root-route test to assert the application's intentional redirect to Login.
